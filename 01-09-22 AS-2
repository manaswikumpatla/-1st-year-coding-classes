#include <stdio.h>

int main()
{
    int n,result;
    scanf("%d",&n);
    result=fact(n);
    printf("factorial is %d",result);
}
int fact(int x)
{
    int i,fact=1;
    for(i=1;i<=x;i++)
    {
        fact=fact*i;
    }
    return(fact);
}
OUTPUT:
5
120
